module.exports =
  getEditor: ->
    @editor

  getEditorElement: ->
    @editorElement

  setEditor: (editor) ->
    @editor = editor

  setEditorElement: (editorElement) ->
    @editorElement = editorElement
